/*
  Supabase browser configuration.
  The anon/public key is safe to expose in a browser ONLY when Row Level Security
  is enabled and the policies in supabase-schema.sql are installed.
*/
window.SUPABASE_URL = "YOUR_SUPABASE_URL";
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
